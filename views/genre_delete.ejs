<h1><%= locals.title %>: <%= locals.genre.name %></h1>

<% if (locals.genre_books.length) { %>
    <p><strong>Delete the following books before attempting to delete this genre.</strong></p>
    <div style="margin-left:20px;margin-top:20px">
        <h4>Books</h4>
        <dl>
            <% locals.genre_books.forEach((book) => { %>
                <dt>
                    <a href="<%= book.url %>"><%= book.title%></a>
                </dt>
                <dd><%= book.summary %></dd>
            <% }) %>
        </dl>
    </div>
<% } else { %>
    <p>Do you really want to delete this Genre?</p>

    <form method="POST" action="">
        <div class="form-group">
            <input id="authorid" class="form-control" type="hidden" name="genreid" required value="<%= locals.genre._id %>" />
        </div>
        <button class="btn btn-primary" type="submit">Delete</button>
    </form>
<% } %>